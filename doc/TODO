A few ideas for future improvements:

* Download the Uniprot database for work offline, so we avoid the some-
  times unreliable download of sequences.

* Make downloading OrthoDB flexible for the database version. At the
  moment, the script is fixed to 10v1.
   